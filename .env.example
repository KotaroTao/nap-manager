# ===========================================
# デンタルNAPマネージャー 環境変数設定
# ===========================================
# このファイルを .env.local（開発用）または .env（本番用）にコピーして使用してください
# cp .env.example .env.local

# ===========================================
# データベース設定 (必須)
# ===========================================
# PostgreSQL接続URL
# 形式: postgresql://[ユーザー名]:[パスワード]@[ホスト]:[ポート]/[データベース名]
DATABASE_URL="postgresql://postgres:password@localhost:5432/nap_manager"

# ===========================================
# NextAuth.js 認証設定 (必須)
# ===========================================
# 認証シークレット（ランダムな文字列を生成: openssl rand -base64 32）
AUTH_SECRET="your-super-secret-key-change-this-in-production"

# アプリケーションURL（本番環境では実際のドメインに変更）
NEXTAUTH_URL="http://localhost:3000"

# ===========================================
# メール送信設定 (オプション)
# ===========================================
# メール送信プロバイダー: console | smtp | sendgrid
EMAIL_PROVIDER="console"

# --- SMTP設定 (EMAIL_PROVIDER=smtp の場合) ---
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-smtp-user"
SMTP_PASSWORD="your-smtp-password"
SMTP_FROM="noreply@example.com"

# --- SendGrid設定 (EMAIL_PROVIDER=sendgrid の場合) ---
SENDGRID_API_KEY="your-sendgrid-api-key"
SENDGRID_FROM="noreply@example.com"

# ===========================================
# アプリケーション設定 (オプション)
# ===========================================
# Node環境（development | production）
NODE_ENV="development"

# ログレベル（debug | info | warn | error）
LOG_LEVEL="info"
