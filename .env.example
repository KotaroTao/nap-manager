# ===========================================
# デンタルNAPマネージャー 環境変数設定
# ===========================================
# このファイルを .env にコピーして使用してください
# Windows: copy .env.example .env
# Mac/Linux: cp .env.example .env

# ===========================================
# 必須設定（この3つだけ変更すればOK）
# ===========================================

# 認証シークレット（32文字以上のランダムな文字列）
AUTH_SECRET="MySecretKey12345678901234567890ab"

# データベースパスワード（任意の文字列）
DB_PASSWORD="postgres"

# アプリケーションURL（ローカルならそのままでOK）
NEXTAUTH_URL="http://localhost:3000"

# ===========================================
# 以下は変更不要
# ===========================================
NODE_ENV="production"
EMAIL_PROVIDER="console"
